{{- range $name, $connector := .Values.connectors }}
{{- if $connector.enabled }}
{{- $ingestMode := $connector.ingestMode | default "http" }}
{{- $es := $connector.externalSecret | default dict }}
{{- /*
Read-only root filesystem ($roRoot, below).

Five of these connectors -- arxiv, gmail, imap, outlook, semantic-scholar --
fork pdftotext/tesseract/pandoc on bytes an attacker chose. A read-only root
means code that gets execution inside one cannot leave a dropper behind on the
image layer; what stays writable is the /tmp emptyDir, /state, and (in
filesystem ingest mode) the staging PVC.

Spelled out here rather than pulled from .Values.containerSecurityContext:
that value belongs to the scanner and switching to it would silently apply the
scanner's other settings to every connector.

The writable /tmp is needed by the Go side, not by the enrichers. Driven the
way connector/enrich drives them (fixed argv, document on stdin, text on
stdout) all three tools run to completion with the entire root read-only and
no /tmp at all; scripts/test-enricher-runtime.sh asserts that on the real
image. What does need a temp dir is connector/http_backend.go NewItem, which
calls os.MkdirTemp("") for every item on the default http ingest path. TMPDIR
is set explicitly so that dependency is visible rather than resting on /tmp
being os.TempDir()'s fallback -- and so any enricher that consults TMPDIR
lands on the same emptyDir.

Turn off chart-wide with readOnlyRootFilesystem.enabled=false, or per
connector with connectors.<name>.readOnlyRootFilesystem=false.
*/}}
{{- $roRoot := include "glovebox.readOnlyRootFilesystem" (dict "workload" $connector "root" $) }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "glovebox.fullname" $ }}-{{ $name }}
  labels:
    {{- include "glovebox.labels" $ | nindent 4 }}
    app.kubernetes.io/component: connector
    glovebox.io/connector: {{ $name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "glovebox.name" $ }}
      app.kubernetes.io/instance: {{ $.Release.Name }}
      app.kubernetes.io/component: connector
      glovebox.io/connector: {{ $name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "glovebox.name" $ }}
        app.kubernetes.io/instance: {{ $.Release.Name }}
        app.kubernetes.io/component: connector
        glovebox.io/connector: {{ $name }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/connector-configmap.yaml") $ | sha256sum }}
        {{- if $.Values.metrics.annotations }}
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
        {{- end }}
    spec:
      serviceAccountName: {{ include "glovebox.serviceAccountName" $ }}
      {{- with (include "glovebox.runtimeClassName" (dict "workload" $connector "root" $)) }}
      runtimeClassName: {{ . | quote }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        fsGroup: 65534
        {{- with (include "glovebox.seccompProfile" $) }}
        {{- . | nindent 8 }}
        {{- end }}
      containers:
        - name: {{ $name }}
          image: "{{ $connector.image.repository }}:{{ $connector.image.tag }}"
          imagePullPolicy: {{ $connector.image.pullPolicy | default "IfNotPresent" }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            {{- if $roRoot }}
            # Writable surface is the /tmp emptyDir, /state, and the staging
            # PVC in filesystem ingest mode. See the note at the top of this
            # template.
            readOnlyRootFilesystem: true
            {{- end }}
          env:
            {{- if $roRoot }}
            - name: TMPDIR
              value: /tmp
            {{- end }}
            {{- if eq $ingestMode "http" }}
            {{- if ne $.Values.ingest.tls.mode "disabled" }}
            - name: GLOVEBOX_INGEST_URL
              value: "https://{{ include "glovebox.fullname" $ }}-ingest:{{ $.Values.ingest.tls.port }}/v1/ingest"
            # The framework switches to a client-certificate transport when
            # all three are present. A partial set is an error rather than a
            # silent fall back to plaintext.
            - name: GLOVEBOX_INGEST_CA
              value: /etc/ingest-tls/ca.crt
            - name: GLOVEBOX_INGEST_CLIENT_CERT
              value: /etc/ingest-tls/tls.crt
            - name: GLOVEBOX_INGEST_CLIENT_KEY
              value: /etc/ingest-tls/tls.key
            {{- else }}
            - name: GLOVEBOX_INGEST_URL
              value: "http://{{ include "glovebox.fullname" $ }}-ingest:{{ $.Values.config.ingest.port }}/v1/ingest"
            {{- end }}
            {{- else }}
            - name: GLOVEBOX_STAGING_DIR
              value: /data/glovebox/staging
            {{- end }}
            - name: GLOVEBOX_STATE_DIR
              value: /state
            - name: GLOVEBOX_CONNECTOR_CONFIG
              value: /etc/connector/config.json
            {{- range $key, $value := $connector.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          {{- if or $connector.secrets $es.enabled }}
          envFrom:
            {{- if $connector.secrets }}
            - secretRef:
                name: {{ $connector.secrets }}
            {{- end }}
            {{- if $es.enabled }}
            - secretRef:
                name: {{ include "glovebox.fullname" $ }}-{{ $name }}-secret
            {{- end }}
          {{- end }}
          ports:
            - name: health
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml $connector.resources | nindent 12 }}
          volumeMounts:
            {{- if eq $ingestMode "filesystem" }}
            - name: staging
              mountPath: /data/glovebox/staging
            {{- end }}
            - name: state
              mountPath: /state
            - name: config
              mountPath: /etc/connector
              readOnly: true
            {{- if and (eq $ingestMode "http") (ne $.Values.ingest.tls.mode "disabled") }}
            - name: ingest-tls
              mountPath: /etc/ingest-tls
              readOnly: true
            {{- end }}
            {{- if $roRoot }}
            - name: tmp
              mountPath: /tmp
            {{- end }}
      {{- with ($connector.nodeSelector | default $.Values.nodeSelector) }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with ($connector.affinity | default $.Values.affinity) }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with ($connector.tolerations | default $.Values.tolerations) }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        {{- if eq $ingestMode "filesystem" }}
        - name: staging
          persistentVolumeClaim:
            claimName: {{ $.Values.persistence.staging.existingClaim | default (printf "%s-staging" (include "glovebox.fullname" $)) }}
        {{- end }}
        - name: state
          persistentVolumeClaim:
            claimName: {{ $connector.state.existingClaim | default (printf "%s-%s-state" (include "glovebox.fullname" $) $name) }}
        - name: config
          configMap:
            name: {{ include "glovebox.fullname" $ }}-{{ $name }}-config
        {{- if and (eq $ingestMode "http") (ne $.Values.ingest.tls.mode "disabled") }}
        - name: ingest-tls
          secret:
            secretName: {{ include "glovebox.fullname" $ }}-{{ $name }}-ingest-client-tls
        {{- end }}
        {{- if $roRoot }}
        # sizeLimit, not the unbounded emptyDir default: a document that
        # spools without limit should evict its own pod, not fill the node.
        - name: tmp
          emptyDir:
            sizeLimit: {{ include "glovebox.tmpSizeLimit" (dict "workload" $connector "root" $) }}
        {{- end }}
{{- end }}
{{- end }}
